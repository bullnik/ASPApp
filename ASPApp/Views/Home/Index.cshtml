@{
    ViewData["Title"] = "Home Page";
}

<div class="bg-white shadow-sm mx-5">

    <div class="input-group p-3 mb-3">
        <input id="search" type="search" class="form-control rounded" placeholder="Search"
                aria-label="Search" aria-describedby="search-addon"/>
        <button id="dirsearch" type="button" class="btn btn-outline-primary">Search</button>
    </div>

    <div class="text-center p-3 mb-3">
      <div class="spinner-border" role="status">
        <span class="visually-hidden">Loading...</span>
      </div>
    </div>

    <div id="dirsearchresults"></div>

    <div class="text-center p-3 mb-3">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>

    <div id="reqsearchresults"></div>

</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#dirsearch').click(function (e) {
                e.preventDefault();
                var name = $('#search').val();
                console.log(name);
                $('#dirsearchresults').load('@Url.Action("SearchDirection", "Home")?request=' + name)
            });
        });
    </script>

    <script>
        $(document).ready(function () {
            $('#directionCheckbox').click(function (e) {
                e.preventDefault();
                var name = $('#search').val();
                console.log(name);
                $('#reqsearchresults').load('@Url.Action("SearchRequirement", "Home")?request=' + name)
            });
        });
    </script>

    <script>
        function myFunction() {
            var checkBox = document.getElementById("directionCheckbox");
            $('#reqsearchresults').load('@Url.Action("SearchRequirement", "Home")?request=' + name)
        }
    </script>
}
